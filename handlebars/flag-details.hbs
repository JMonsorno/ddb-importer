<form>
  <header class="sheet-header">
    <h1 class="item-title">{{name}}</h1>
  </header>

  <div class="ddb-importer-demo-flags">
    {{#each flags}}
    <h2 class="ddb-importer-demo-flags">data.flags.{{@key}}:</h2>
    {{#each this}}
    <div class="ddb-importer-demo-flags">
      <li class="ddb-importer-demo-flags">{{this.key}}: {{this.value}}</li>
    </div>
    {{/each}}
    {{/each}}
  </div>
</form>
